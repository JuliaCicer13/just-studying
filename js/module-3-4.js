function logItems(array){
    let styles = [];
    for (i = styles; i <= styles; i ++) {
        styles.push('rock-n-roll');
        if (styles.includes("blues")){
           styles.toggle("classic");
           return styles;
        }
    }
}


logItems(`1-jazz:${1-jazz}`, `2-blues: ${2-blues}`);